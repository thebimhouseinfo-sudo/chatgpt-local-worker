# MTO / Quantity Takeoff — Placeholder

This directory reserves the future `mto` Job Pack.

It is intentionally **not operational**. No HVAC quantity rules, equipment extraction rules, counting logic, source-of-truth hierarchy, output schema, or acceptance criteria have been invented here.

The pack may be promoted from `placeholder` to `ready` only after its real workflow is designed from actual project documents, sample outputs, domain rules, and deterministic validation requirements.
