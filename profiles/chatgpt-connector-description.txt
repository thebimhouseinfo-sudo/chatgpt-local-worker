GPTWorker is a trusted local worker with controlled Job Packs. Use this connection only through the explicit @gptworker flow or GPTWorker system commands.

Bare @gptworker / connector mention:
- If the user invokes the GPTWorker mention/pill with no task text, treat that as bare @gptworker even if the transcript UI shows only the gptworker badge.
- Call job_list exactly once. If the UI does not expose literal mention text, use the canonical activation_request "@gptworker".
- Render the currently available Jobs as a numbered list, including custom Jobs.
- Then show exactly: "Hãy chọn Job và đưa tôi thư mục làm việc để bắt đầu, hoặc gõ gptworker/ để xem các system commands."
- Do not replace this with a generic "send task + path" message.

System commands:
- gptworker/ shows the fixed system-command menu only.
- gptworker/help and the harmless spacing variant gptworker/ help are chat-only help. Do not call any tool for them; return the prewritten GPTWorker help from server instructions.
- The same harmless whitespace around "/" should not turn a GPTWorker system command into a work request.

Work activation:
- A fresh task + local path without an explicit @gptworker flow is not GPTWorker activation.
- After @gptworker has admitted/nominated a Job, show JOB + FOLDER and wait for explicit confirmation before workspace execution.
- Do not ask the user to repeat Job/Folder/task when already clear inside the active @gptworker flow.

Secure MCP Tunnel setup: in ChatGPT use Connection: Tunnel, select or paste the tunnel ID, then Scan Tools. Do not enter the local http://127.0.0.1 MCP URL.
